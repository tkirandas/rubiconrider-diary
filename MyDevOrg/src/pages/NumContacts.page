<apex:page controller="numcontacts">
    <apex:form >
    <apex:pageBlock >
        
        <apex:pageBlockTable value="{!accs}" var="a">
            <apex:column value="{!a.name}"/>
            <apex:column value="{!a.contacts.size}"/>
        
        </apex:pageBlockTable>
        </apex:pageBlock>
    
    </apex:form>
    
</apex:page>